using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SavedPosition : MonoBehaviour
{
    private CharacterBehaviour character;
    private CharacterHealth characterHP;
    // Start is called before the first frame update
    void Start()
    {
        /*character = GetComponent<CharacterBehaviour>();
        if (GameManagement.instance.player == null)
            GameManagement.instance.player = character;
        else
            character = GameManagement.instance.player;
        if (GameManagement.instance.playerHP == null)
            GameManagement.instance.playerHP = characterHP;
        else
            characterHP = GameManagement.instance.playerHP;
        //transform.position = GameManagement.instance.position;
        -*/
    }

    
}
